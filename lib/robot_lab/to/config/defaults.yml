defaults:
  provider: openai
  model: gpt-5.5
  max_tool_rounds: 100
  max_consecutive_failures: 3
  max_retries: 2
  max_submit_nudges: 1
  verify_timeout: 600
  commit_format: default
  run_dir: .robot_lab_to
  local_guards: false
  stream: true
  debug: false

development:
  debug: false

test:
  debug: false

production:
  debug: false
